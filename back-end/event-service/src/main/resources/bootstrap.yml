spring:
  application:
    name: event-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://config:8888
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 10000
        maxAttempts: 20
server:
  port: 8081