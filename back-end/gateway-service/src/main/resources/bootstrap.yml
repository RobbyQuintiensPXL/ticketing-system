spring:
  application:
    name: api-gateway
  cloud:
    consul:
      discovery:
        instance-id: "${spring.application.name}:${random.int[1,99999]}"
        register: false
        register-health-check: false
        tags: api-gateway
      host: localhost
      port: 8500
    gateway:
      discovery:
        locator:
          enabled: true
#      default-filters:
#        - TokenRelay
      routes:
        - id: event-service
          uri: lb://event-service
          predicates:
            - Path=/event/**
          filters:
            - RewritePath=/event/(?<path>.*), /$\{path}
        - id: ticket-service
          uri: lb://ticket-service
          predicates:
            - Path=/ticket/**
          filters:
            - RewritePath=/ticket/(?<path>.*), /$\{path}
            - RemoveRequestHeader= Cookie,Set-Cookie

#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: https://dev-7mgwq79y.eu.auth0.com/
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-7mgwq79y.eu.auth0.com/
#      client:
#        registration:
#          auth0:
#            client-id: Ny5lpiTnQWBa7OKC3GHEquZdupRm4pjt
#            client-secret: DPSUU2UPyTORrUTO9kizc7x-6_3gVKsGXBCQXjp0Fa__LexCSp4vqVvTOO-8JFKf
#            authorization-grant-type: authorization_code
#            redirect-uri: 'http://localhost:9080/login/oauth2/code/auth0'
#            scope: openid,profile,email,user,office
#        provider:
#          auth0:
#            issuer-uri: https://dev-7mgwq79y.eu.auth0.com/


  main:
    allow-bean-definition-overriding: true

auth0:
  audience: https://jevents.be


management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: '*'
        allowed-headers: '*'
        allowed-methods: '*'

server:
  port: 9080
